<div class="header row">
  <div class="col-10">
    hihi
  </div>
  <div class="col-2">
    <% if user_signed_in? %>
      <div class="">
        <span> Hello, <strong><%= current_user.teacher.full_name %></strong> </span>
        <span><%= current_user.role %></span>
      </div>
      <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
    <% else %>
      <%= link_to('Login', new_user_session_path) %>
    <% end %>
  </div>
</div>

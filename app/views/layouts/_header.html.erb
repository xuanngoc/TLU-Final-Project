<div class="header row">
  <div class="col-10">
    hihi
  </div>
  <div class="col-2">
    <% if user_signed_in? %>
      <div class="">
        <h4> Hello, <strong><%= current_user.full_name %></strong> </h4>
        <span><%= current_user.role %></span>
      </div>
      <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
    <% else %>
      <%= link_to('Login', new_user_session_path) %>
    <% end %>
  </div>
</div>
